# Dependencies and build output are produced inside the image.
node_modules
.svelte-kit
build

# Never ship local secrets or VCS metadata.
# `.env` holds real credentials and must not end up in a layer.
.env
.env.*
!.env.example
.git
.github

# Local tooling, editor state and test artifacts.
.vscode
.roo
test-results
playwright-report
ci-results
*.log
static/interface